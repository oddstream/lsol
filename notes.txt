LÖVE Solitaire
	is this the one you like playing The Most?

	could use point {x,y} and rectangle {x, y, width, height}
	but that would be inefficient
	just stick to the same as LÖVE (and raylib) and use x,y,width,height



https://github.com/love2d/love-android
	clone this to love-android

https://github.com/love2d/love-android/wiki/Game-Packaging

https://developer.android.com/studio/build/building-cmdline

https://docs.microsoft.com/en-us/xamarin/android/deploy-test/signing/manually-signing-the-apk

https://github.com/EmmanuelOga/columns
https://github.com/a327ex
	example LÖVE games

https://github.com/rxi/json.lua
	json library

https://github.com/gvx/bitser
	bitser library

# adb
https://developer.android.com/studio/command-line/adb

alias adb=$HOME/Android/Sdk/platform-tools/adb

adb devices -l

adb -s ZY223Z8MVL push lsola.love sdcard

adb -s ZY223Z8MVL push lsola.apk sdcard
adb -s HVA2JH79 push lsola.apk sdcard
adb -s emulator-5554 push lsola.apk sdcard

adb -s HVA2JH79 install lsola.apk
adb -s ZY223Z8MVL install lsola.apk
adb -s emulator-5554 install lsola.apk

adb -s HVA2JH79 shell pm uninstall com.oddstream.lovesolitaire
adb -s ZY223Z8MVL shell pm uninstall com.oddstream.lovesolitaire
adb -s emulator-5554 shell pm uninstall com.oddstream.lovesolitaire

Go-style indenting with tabs
function and variable names in cascalCase
class names in PascalCase
constants in UPPER_CASE
    includes string constants like FAN_NONE, MOVE_NONE


LÖVE game loop https://github.com/a327ex/blog/issues/15
https://github.com/a327ex/BYTEPATH/tree/master/tutorial/tutorial-code/05%20-%20Game%20Basics

SUBCLASS LAYER
CARD POSITIONING LAYER
    Pile and Card have a grp, that has x,y position, so we don't need one
    use csol as a model?
    Pile.Refan()
    Pile.PosAfter(c)
    setup pos1 & pos2 when setting Pile Baize pos
SCRIPTING LAYER
INPUT LAYER
GAME MANAGEMENT LAYER

_G.BAIZE.piles
_G.BAIZE.deck
_G.BAIZE.cells -- etc
_G.BAIZE.stock -- only one
_G.BAIZE.waste -- only one
_G.BAIZE.script

The Cell,Discard,Foundation,Reserve,Stock,Tableau,Waste interface:
    Cell.New(o)
    Cell:CanAcceptCard(c)
    Cell:CanAcceptTail(tail)
    Cell:TailTapped()
    Cell:Collect()  -- only implemented in Foundation piles
    Cell:Conformant()
    Cell:Complete()
    Cell:UnsortedPairs()

The variant script interface:
    Info()
    BuildPiles()
    StartGame()
    AfterMove()
    TailMoveError()
    TailAppendError()
    UnsortedPairs()
    TailTapped()
    PileTapped()
...unlike gosol, record of pile subtypes will be kept in _G.BAIZE, eg _G.BAIZE.cells

Pile.category
Pile.slot.x
Pile.slot.y
Pile.fanType
Pile.moveType
Pile.cards
Pile.label
Pile.symbol (recycle rune)
Pile.grp (x, y etc)

https://docs.coronalabs.com/api/library/display/safeActualContentHeight.html

https://docs.coronalabs.com/api/library/display/safeScreenOriginX.html

ZeroBraneStudio
Can't find love2d executable in any of the following folders: /home/gilbert/android-studio/jre/bin, /home/gilbert/go/bin, /usr/local/sbin, /usr/local/bin, /usr/sbin, /usr/bin, /sbin, /bin, /usr/games, /usr/local/games, /snap/bin, /snap/bin, /usr/local/go/bin, /home/gilbert/, /home/bin

 If löve executable is in the same folder where your game folders are or in one of the folders listed in PATH, it will be found by the IDE. If the executable is in a different location, you may need to specify this location in cfg/user.lua file (see cfg/user-sample.lua for details on how this is done).
